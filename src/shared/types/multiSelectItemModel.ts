export type MultiSelectItemModel = {
    value: string | number;
    label: string;
    description?: string;
    imageSrc?: string;
  };

  export type SelectedItemModel = {
    label: string;
    value: string | number;
  };